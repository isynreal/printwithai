# Vercel server-side environment variable. Never prefix this with VITE_.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-5.6-luna

# Supabase project settings. These are safe browser-facing values when RLS is enabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
